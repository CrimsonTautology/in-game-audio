<h1><%= @directory.full_path %></h1>

<%= link_to "Add a new song...", new_song_path %>

<ul>
  <% unless @directory.root? %>
    <li>
      <%= link_to "..", @directory.parent %>
    </li>
  <% end %>
  <% @subdirectories.each do |sub| %>
    <li>
      <%= link_to sub.path_name, sub %>
    </li>
  <% end %>
  <% @songs.each do |song| %>
    <li>
      <%= link_to song.name, song %>
    </li>
  <% end %>
</ul>

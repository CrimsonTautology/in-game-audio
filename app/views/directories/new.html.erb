<%= simple_form_for @directory do |f| %>
  <%= f.input :name, required: true  %>
  <%= f.input :description %>

  <%= f.input :parent_id, collection: @directories, label_method: :to_label, required: true %>


  <%= f.button :submit %>
<% end %>
